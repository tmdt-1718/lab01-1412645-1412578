<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>Webapp</title>
		<%= csrf_meta_tags %>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>
	<body>
		<nav class="navbar navbar-light bg-light navbar-default-custom navbar-expand-lg">
			<div class="container">
			<button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
			</button>
			<img class="navbar-brand logo" src="<%= asset_path('logo.png') %>">
			<div class="navbar-collapse collapse" id="navbarSupportedContent">
				<ul class="navbar-nav mr-auto">
					<li class="nav-item <%= isMenuActive('/home') %>"><a class="nav-link" href="<%= home_path %>">Home</a></li>
					<li class="nav-item <%= isMenuActive('/albums')  %>"><a class="nav-link" href="<%= albums_path %>">Albums</a></li>
					<li class="nav-item <%= isMenuActive('/blog')  %>"><a class="nav-link" href="<%= blog_path %>">Blog</a></li>
					<li class="nav-item <%= isMenuActive('/about')  %>"><a class="nav-link" href="<%= about_path %>">About</a></li>
				</ul>
			</div>
			<form class="form-inline my-2 my-lg-0">
		      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Đăng ký</button>
		    </form>
		</div>
		</nav>
		<div class="container container-content">
			<ol class="breadcrumb">
				<% @breadcums.each_with_index do |item, index| %>
				<li class="breadcrumb-item <%= index==@breadcums.length-1 ? "" : "active" %>">
					<% if index==@breadcums.length-1 %>
					<%= item[:title] %>
					<% else %>
					<a href="<%= item[:url] %>"><%= item[:title] %></a>
					<% end %>
				</li>
				<% end %>
			</ol>
			<div class="row">
				<div class="col-sm-3">
					
				</div>
				<div class="col-sm-9">
					<%= yield %>
				</div>
			</div>
		</div>
		<footer>
			<div class="container">
				<p>Copyright 2017 by Trí &amp; Vũ. All Rights Reserved.</p>
			</div>
		</footer>
	</body>
</html>